<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:security="http://www.springframework.org/schema/security"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security.xsd">
    <!--配置spring安全认证服务-->

    <!--配置不过滤的资源（静态资源和与登陆相关的资源）-->
    <!--security:安全，配置none不过滤，pattern：资源路径-->
    <security:http security="none" pattern="/login.jsp"/>
    <security:http security="none" pattern="/failer.jsp"/>
    <!--因为下面配置了默认目标路径为index.jsp,所以服务器一启动就会打开index页面，这时必须放开index页面，不然服务器一启动
    就会报accessExecution-->
    <security:http security="none" pattern="/index.jsp"/>
    <security:http security="none" pattern="/css/**"/>
    <security:http security="none" pattern="/js/**"/>
    <security:http security="none" pattern="/plugins/**"/>

    <!--spring_security基本配置-->

    <!--auto-config,配置为true会自动加载一些默认的配置，
    不用自己编写登录的页面，框架提供默认登录页面
    use-expressions：是否使用SPEL表达式（没学习过）-->
    <security:http auto-config="true" use-expressions="true">
        <!--以下内容都是在配置auto-config，下面的标签也是由http标签解析的-->

        <!--配置自动登陆,cookie会存储7天-->
        <security:remember-me remember-me-parameter="remember-me" token-repository-ref="jdbcTokenRepository" token-validity-seconds="604800"/>

        <!-- 配置具体的拦截的规则 pattern="请求路径的规则" access="访问系统的人，必须有ROLE_USER的角色" -->
        <security:intercept-url pattern="/**" access="hasRole('ROLE_USER')"/>
        <!-- 定义跳转的具体的页面,填写登陆相关表单数据（参数） -->
        <security:form-login
                default-target-url="/index.jsp"
                login-page="/login.jsp"
                login-processing-url="/login"
                authentication-success-forward-url="/pages/main.jsp"
                authentication-failure-url="/failer.jsp"
                username-parameter="username"
                password-parameter="password"
        />
        <!--关闭跨域请求-->
        <security:csrf disabled="true"/>
        <!--注销用户-->
        <security:logout invalidate-session="true" logout-url="/logout" logout-success-url="/login.jsp"/>
    </security:http>

    <!--配置数据库-->
    <security:authentication-manager>
        <security:authentication-provider user-service-ref="userService">
            <!--可以配置加密方式-->
            <security:password-encoder ref="passwordEncoder"/>
        </security:authentication-provider>
    </security:authentication-manager>
    <!--&lt;!&ndash;配置加密类&ndash;&gt;-->
    <bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <bean id="jdbcTokenRepository" class="org.springframework.security.web.authentication.rememberme.JdbcTokenRepositoryImpl">
        <property name="dataSource" ref="dataSource"/>
        <!--第一次使用自动生成固定PERSISTENT_LOGINS表保存记录，如果哦已经有此表则不能再配置此项-->
        <!--<property name="createTableOnStartup" value="true"/>-->
    </bean>

</beans>
